# Build output
target/
*.jar
!docker/spark/jars/.gitkeep

# Python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/

# Node / bun (the SPARQL console). bun.lock IS committed, so the image builds reproducibly.
node_modules/
dist/
.vite/
*.tsbuildinfo

# Local state
.env
docker/spark/jars/*.jar
.cache/

# Editors
.idea/
.vscode/

# Browser-automation scratch (snapshots, console dumps) from driving the consoles.
.playwright-mcp/

# Git worktrees created by tooling; never part of the project tree.
.claude/worktrees/
